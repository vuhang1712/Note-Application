<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.teamfighttatic.HUS_OOP_ToDoApplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AlarmActivity"></activity>
        <activity android:name=".activity.FilterTag" />
        <activity android:name=".activity.ListIntTodoList" />
        <activity android:name=".activity.NoteDetailActivity" />
        <activity android:name=".activity.TodoActivity" />
        <activity android:name=".activity.TagActivity" />
        <activity android:name=".activity.AddTaskActivity" />
        <activity android:name=".activity.NoteActivity" />
        <activity android:name=".activity.MainTaskDetailActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>